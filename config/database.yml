default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: debug_log_development

test:
  <<: *default
  database: debug_log_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
  